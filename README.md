# scrcpy 增强版（scrcpy-enh）

基于 [scrcpy 4.1](https://github.com/Genymobile/scrcpy) 的增强构建，面向「电脑 ↔ Android 移动设备」投屏协同场景，在官方版本之上提供自适应码率、状态显示、图片剪贴板等实用增强功能。

> **v1.5 已正式发布**（master 分支）——[Release 下载](https://github.com/kinewe/PC-kinewe-yinmo/releases/tag/v1.5)（zip 含全部运行库，解压即用）
>
> 历史版本：[v1.4](https://github.com/kinewe/PC-kinewe-yinmo/releases/tag/v1.4) 及更早见 Release 列表

---

## ✨ 核心功能总览

| 特性 | 版本 | 说明 |
|---|---|---|
| 🔄 **投屏循环 + 插线检测** | v1.1 | USB 拔插自动切换有线/无线，无需手动操作 |
| 🎹 **uhid 键盘伪装** | v1.2 | 电脑键盘被 Android 认作「外部物理键盘」，可直接打中文 |
| 📋 **图片剪贴板同步** | v1.2 | 电脑复制图片/文本 → 自动同步手机剪贴板（合并 PR #6676） |
| 🖼️ **Ctrl+G 存相册** | v1.2 | 投屏中按 Ctrl+G 把剪贴板原图保存到手机相册 |
| 📐 **动态规格分配** | v1.2.1 | 插线时读取设备分辨率/刷新率，动态分配串流规格 |
| ⚡ **ABR 双维自适应** | v1.3 | 码率 + 帧率双维自适应降级/恢复，压力下保持跟手 |
| 📊 **投屏状态控件** | v1.3.1 | 右上角实时显示 帧率/码率/连接模式，Alt+拖动可移动，Ctrl+F 开关 |
| ⌨️ **TSF 空文档屏蔽** | v1.5 | 输入法不抢键、切回电脑应用永远中文——QQ/Word/UWP 输入模式残留根治 |

---

## 📊 v1.3.1 新增：投屏状态控件

投屏窗口**右上角**实时显示一行状态：

```
10/60fps 15M WIFI
↑实际/档位 ↑码率 ↑模式
```

| 项目 | 含义 |
|---|---|
| **实际帧率** | 当前画面真实渲染帧率（500ms 滑动窗口）——静止时平板系统会降频（如 10fps），滑动/动画时自动回满 |
| **档位帧率** | ABR 当前目标档位（60/90/120 等，编码上限）——「10/60fps」= 手机当前实际送来 10fps、目标上限 60fps；实际帧率由手机端决定（静止时系统省电降频，滑动/动画自动回满），不代表性能不足 |
| **码率** | ABR 实时码率（≥1M 显示 M，<1M 显示 k）——降档/恢复肉眼可见 |
| **模式** | `USB`（有线）/ `WIFI`（无线）——拔插线自动切换显示 |

### ✋ 移动控件

**按住 Alt + 鼠标左键拖动**即可把控件移到任意位置（自动限制在窗口内，不会拖出视野）。重启后回到默认右上角。

---

## ⌨️ 快捷键一览

| 快捷键 | 功能 |
|---|---|
| **Ctrl+G** | 把电脑剪贴板里的**原图保存到手机相册**（无损原图；中文输入法下 Ctrl 组合键直达，不被拦截） |
| **Ctrl+V** | 电脑复制图片/文本 → 手机剪贴板（配合手机长按粘贴） |
| **Ctrl+F** | 显示/隐藏右上角状态控件（开关切换，会话内记忆，重启后恢复显示） |
| **Alt + 左键拖动** | 移动右上角状态控件位置 |
| **Q** | 退出投屏（拔线后自动切换无线继续，Q 彻底退出） |
| 普通按键 | uhid 键盘直接输入到手机（含中文） |

---

## ⚡ v1.3 里程碑：ABR 双维自适应调教

### 设计哲学：三全目标

**清晰度 + 高帧率 + 跟手** 缺一不可——绝不为跟手牺牲画质（拒绝固定低分辨率/低帧率兜底）。

### 双维缓冲机制

| 维度 | 作用 | 说明 |
|---|---|---|
| **码率缓冲** | 编码吞吐调节 | 帧内容大/轻微超载时降码率（数据量↓），下限 1M |
| **帧率缓冲** | 编码次数调节 | 码率缓冲不够时 fps 120→90 一同分担（ME 成本↓25%） |

**90fps 缓冲搭档档（v1.3 核心）**：
- **码率缓冲时**：complex 帧/轻微超载触发降码率，fps 同步 **120→90**——双缓冲一同分担（顶到 120 后只靠码率缓冲不够跟手）
- **快速降档时**：真超载（≥90ms/窗口确认）fps **120→60→30 一步到底**——90 不做降档中间台阶
- **恢复时**：30→60→90→120 平滑升档（90 是恢复台阶）

### 检测链路（四级递进）

| 信号 | 阈值 | 响应 |
|---|---|---|
| **趋势检测**（GCC Trendline） | 3 帧连续递增 +2ms 且 >10ms | 事前温和降码率 ×0.7（23ms 即响应） |
| **instant 瞬时** | 单帧 >25ms | 降码率（<100ms 温和 ×0.7 / ≥100ms 激进 ×0.3） |
| **窗口确认** | 50ms 窗口 delayDelta>30ms 占 5/7 | 确认超载，码率/fps 降级 |
| **fps 快速降档** | instant ≥90ms 或恢复期反复超载 | 120→60→30（跳过 90） |

### 恢复策略

| 项目 | 参数 |
|---|---|
| fps 恢复 | stable 300ms + watch 300ms + confirmed 信用（30→120 ~0.9s） |
| 码率恢复 | <16M 每 100ms / ≥16M 每 200ms / ceiling 释放 300ms（糊→能看 ~0.5s） |
| 恢复防抖 | 偶发大帧（I 帧/壁纸 95KB）单次免疫，连续 2 次确认才打断 |
| 爬升豁免 | 码率爬升期轻微超载/complex 不打断（climbing 判定） |
| 降档退避 | 3s 内重复降档冷却 ×2（1s→2s→4s→8s 阻尼振荡） |
| 排空豁免 | 降档后 300ms 内不判档（旧队列帧排空） |

---

## ⌨️ v1.5 里程碑：TSF 空文档屏蔽（输入法问题根治）

### 问题背景

uhid 键盘模式下，电脑中文输入法（搜狗/微软拼音）可能拦截按键——旧方案是获焦时把系统键盘布局切成英文（00000409），失焦时再恢复。但 Windows 8+ 在窗口拥有焦点时切换布局是**系统级**的，会污染其他窗口的布局和输入法模式记忆——导致切回 QQ/Word/记事本时输入法停在英文（打字出英文）。

### TSF 空文档屏蔽方案

获焦时在 scrcpy 窗口聚焦一个**空的 TSF 文档**——输入法在投屏窗口内没有组合目标，按键直接放行；**全局键盘布局从头到尾没有变过**，其他窗口的输入法状态完全不受影响。

```
投屏窗口获焦 → 聚焦空 TSF 文档 → 输入法不抢键（uhid 按键直达手机）
切回 QQ/Word → 布局/输入法模式从未被动过 → 永远中文 ✅
```

### 回退开关

TSF 方案默认启用。如遇输入法异常，双击 `TSF旧路径回退.bat` 启动（等效设置环境变量 `SCRCPY_IME_NO_TSF_BLOCK=1`），退回旧的布局切换方案。

---

## 🚀 快速开始

1. 下载 [v1.5 Release](https://github.com/kinewe/PC-kinewe-yinmo/releases/tag/v1.5) 的 zip，解压到任意目录
2. 手机开启 **开发者选项 → USB 调试**（无线使用还需开启「USB 调试（安全设置）」并重启手机）
3. **USB 插线** 双击 `手机投屏.bat` → 自动有线串流（50M/2560/120fps 动态规格）
4. **拔线** → 5 秒后自动切换无线（15M/1920/60fps，WiFi 需与电脑同网段）
5. 投屏中按 `Q` 退出，插回 USB 自动切回有线

> 无线模式首次使用：确保手机与电脑同一局域网，bat 会自动 `adb tcpip 5555` 并保存 IP（`config.txt`），下次免配对。

---

## 🛠️ 构建（源码）

```bash
# Server（Android 端）：
cd server && export JAVA_HOME=/path/to/jdk17
gradle --no-daemon assemble
# 产物：server/build/outputs/apk/debug/server-debug.apk → 复制为 dist/scrcpy-server

# Client（Windows 端）：
cd build && PATH=/f/msys64/mingw64/bin:$PATH ninja
# 产物：build/app/scrcpy.exe → 复制为 dist/scrcpy.exe
```

---

## 📋 常见问题

**Q：投屏有 ~70ms 延迟，正常吗？**
A：正常。手机→电脑串流管线（捕获→编码→传输→解码→显示）物理极限约 70ms（业界标杆水平）。ABR 管理的是**吞吐维度**（超载排队延迟），基线延迟无法消除。

**Q：为什么状态控件显示 10fps？**
A：Android 系统在画面静止时会把虚拟显示降到 10Hz（省电策略）——这是**系统行为**，不是 ABR 问题；滑动/动画时帧率自动回满。控件显示「实际/档位」就是为了让你直观看到这个现象。

**Q：Ctrl+G 没反应？**
A：需要先把图片复制到电脑剪贴板（如微信/浏览器里复制图片），再在投屏窗口按 Ctrl+G。保存后手机相册里会出现原图。

**Q：无线模式卡顿？**
A：无线规格 15M/1920/60fps，受 WiFi 抖动影响（28-30ms 常态抖动已被 ABR 爬升豁免处理）。追求极致体验建议 USB 有线。

---

## 📜 Release 历史

| 版本 | 内容 |
|---|---|
| **v1.5** | TSF 空文档屏蔽（输入法不抢键 + 切回应用永远中文，QQ/Word/UWP 模式残留根治）、TSF旧路径回退.bat 自救开关 |
| **v1.4** | GL 满档减帧修复（120 档真满血）、fps 档位全链路生效、overlay S 字形 + Ctrl+F 开关、全量代码审查加固 |
| v1.3.1 | 投屏状态控件（实际/档位 fps + 实时码率 + USB/WIFI，Alt+拖动可移动） |
| v1.3 | ABR 双维自适应（码率+帧率）、90fps 缓冲搭档档、恢复链提速/防抖、降档退避 |
| v1.2.1 | 动态规格分配（插线读分辨率/刷新率） |
| v1.2 | uhid 键盘伪装中文输入、图片剪贴板同步（PR #6676）、Ctrl+G 存相册 |
| v1.1 | 投屏循环 + USB/WiFi 自动切换 |
| v1.0 | 首个增强构建 |
